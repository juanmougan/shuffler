language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
script:
  - npm test
  - npm run build
sudo: false
before_install:
# install dependencies
install: "pip install -r requirements.txt"  
language: python
python:
  - "2.7"
script: robot tests.robot
